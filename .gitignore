# SSH host keys generated at first run — private material, never commit.
.ssh/

# Local environment: deployment host and key paths. See .env.example.
.env
.env.local

# Compiled binaries.
/canuckpunk
/bin/

# Local SQLite databases.
*.db
*.db-wal
*.db-shm

# Go build and test artifacts.
*.exe
*.test
*.out
coverage.*

# macOS
.DS_Store
