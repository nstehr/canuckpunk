version: "2"
sql:
  - engine: "sqlite"
    queries: "data/queries"
    schema: "data/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_empty_slices: true
